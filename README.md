# scrapper-mtg-set

Scripts to scrap Magic The gathering images and data

To use, install pipenv with python3 and pip.

Install dependencies:

```
pipenv install
```

For exemple, run script scrap-set-cards-visuals.py :

```
pipenv run python3 scrap-set-cards-visuals.py
```