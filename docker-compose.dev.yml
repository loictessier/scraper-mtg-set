version: "3.9"

services:
  scraper-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mtg-scraper-dev
    ports:
      - "5000:5000"
    volumes:
      - ./output:/app/output
    env_file:
      - .env.dev
    command: python -m mtg_scraper_ui.app
